o<x-probe> sub

  #<max_incremental_delta>=#1  ( incremental direction to move )
  #<feed>=#2                   ( feed rate )

  G91
  G38.2 X#<max_incremental_delta> F#<feed>
  (DEBUG, touched off at X#5061)
  G90

o<x-probe> endsub
