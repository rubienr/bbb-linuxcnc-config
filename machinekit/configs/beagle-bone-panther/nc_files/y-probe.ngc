o<y-probe> sub

  #<max_incremental_delta>=#1  ( incremental direction to move )
  #<feed>=#2                   ( feed rate )

  G91
  G38.2 Y#<max_incremental_delta> F#<feed>
  (DEBUG, touched at Y#5062)
  G90

o<y-probe> endsub
